# 1. Winchester plugged in
 - Pipeline
	-- check if there are ffv1 files on the winchester
		> wf stops if fails
	-- create a list from the ffv1 files
		- pick the first item from the list
			- Create a directory stucture
				- Top level directory: BARCODE
					- CONTENT
					- METADATA
			- Copy the current ffv1 file to the CONTENT directory (with RSYNC >> checksum on the fly)
			